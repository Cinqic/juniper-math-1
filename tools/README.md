# tools/

Reserved for future deterministic mathematical tool contracts (the "Cinqic
Calculator" and related runtime — Phase 3 ownership). Phase 0 provides only
the honest CLI placeholder (`python -m juniper_math tool-test`, which
reports "not implemented until Phase 3" and exits non-zero). No tool
runtime is implemented here yet.
