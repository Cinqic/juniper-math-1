# releases/

Release metadata (version, included checkpoint/tokenizer/dataset
identities, changelog) once real releases exist. No releases exist during
Phase 0 — the model has not been built or trained.
