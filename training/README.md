# training/

Training entry points and training-specific supporting infrastructure.
Phase 5 is Smoke Pretraining and is authorized but not started. The
`python -m juniper_math train` command remains an honest placeholder: it
exits non-zero and does not create a training run, checkpoint, or metric.
