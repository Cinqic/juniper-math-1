# training/

Training entry points and training-specific supporting infrastructure
(Phase 1+). Phase 0 provides only the honest CLI placeholder
(`python -m juniper_math train`, which reports "not implemented until
Phase 1" and exits non-zero) — no training loop, optimizer configuration, or
data pipeline exists yet.
